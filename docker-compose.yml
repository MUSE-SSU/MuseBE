version: '3'

services:
    muse:
        build:
            context: .
            dockerfile: ./Dockerfile
            args:
                STAGE: DEV
        ports:
            - 8080:8080
        tty: true
        platform: linux/amd64